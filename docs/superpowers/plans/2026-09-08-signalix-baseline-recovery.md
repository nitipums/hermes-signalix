# Signalix Baseline Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task by task. Use checkbox (`- [ ]`) syntax to track progress. Lite dispatches one bounded wave at a time; Codex Astra (`gpt-6-astra`) implements each source/documentation wave. Lite independently reviews evidence and owns final acceptance. If the execution skill is unavailable, report that limitation before execution; this planning run does not install skills or launch workers.

**Goal:** Freeze feature/policy expansion, establish one attributable baseline, remove the research entry-time leak and Team Facts timestamp defect, preserve all evidence, and give Lite an independently verifiable release candidate.

**Architecture:** Preserve the published canonical read model and `/api/setup-candidates` → `/mvp` decision spine. The producer is `backend/update_data.py:publish_canonical_read_model()` → `backend/mvp_api.py:build_setup_candidates_from_data()` → `backend/read_model_publisher.py:publish_builder_result()`. Requests load/validate the published model in `backend/mvp_routes.py:_handle_canonical_routes()`, overlay bounded sidecar lineage through `backend/canonical_freshness_lineage.py:overlay_latest_intraday_metadata()`, and project through `backend/canonical_setup_projection.py:project_setup_candidates_response()`. Team Facts separately reads the published universe plus bounded Daily/60m observations. Research remains in `.worktrees/research-runner/prototypes/research_to_promotion/`; it never becomes a serving dependency.

**Tech Stack:** Python, pytest/unittest, deterministic Daily OHLCV replay, PostgreSQL read adapters, published JSON read models, vanilla JavaScript/CSS, Docker backend/dashboard, Git worktrees, browser network interception for isolated failure testing.

**Spec:** Owner-approved six-step recovery sequence in the 2026-09-08 request; `AGENTS.md`; `CONTEXT.md`; `docs/START-HERE.md`; `vault/INDEX.md`; `vault/Execution-Pipeline.md`; `vault/Architecture.md`; `vault/Deployment.md`; `vault/Research-Index.md`; `vault/Research-Playbook-Bible.md`; `docs/current/2026-09-08-0915-52w-research-session-closeout.md`; advisory `/tmp/codex_astra_signalix_review_last.md`; product boundary `docs/superpowers/specs/2026-08-30-elliott-trend-trade-setup-design.md`. The request overrides the older Luna worker default and the interrupted closeout's suggestion to continue folds before repairing timing.

**Global Constraints:**

- Freeze features, parameter search, threshold tuning, promotions and broad refactors. Alerts, auto-trading, broker execution and evaluator auto-caller remain OFF/PENDING. Preserve facts-only public/no-auth Team Facts; do not reopen authentication design.
- This planning run creates this file only, with no commit. During implementation, the current owner request authorizes bounded cleanup implementation and local scoped commits **after Lite review**. Commit/push/deploy/restart/DB writes require explicit wave scope. Push, migration, DB writes, ingestion, alerts and trading execution are not authorized. Runtime restart/recreate is confined to the final gate, only if required after Lite verifies source/tests. Read-model republishing is a separate write side effect and is not implicitly authorized by an HTTP probe or restart.
- All existing tracked dirty, untracked and ignored contents are owner-owned until classified. Never reset, stash, clean, force-remove, overwrite unrelated work, or stage whole directories. `git worktree remove --force` is forbidden. Preserve/archive is the cleanup default; leaving a worktree registered is a valid completed retention decision.
- No secrets, `.env*`, credentials, production dumps, production DB contents or ephemeral research result payloads are inspected in planning. Implementation tests use synthetic data. A real replay requires Lite to scope a reviewed read-only research input source or isolated snapshot before execution; no implicit production query permission or fallback data source.
- No simultaneous coding agents in a worktree. The canonical checkout is a production bind mount: even source edits can become observable in a newly loaded process. Lite must establish candidate isolation before production source edits. Use a dedicated recovery worktree after classification, carrying only reviewed source/test patches; never switch/reset the owner checkout.
- Each wave has its own allowlist, evidence and local commit point. Documentation, research source, replay evidence, Team Facts source, retirement and runtime deployment are separate commits/operations. Documentation sync accompanies the bounded change as a separately reviewed docs commit before handoff.
- A failed prerequisite stops dependent work, with `REVISE` or `NOT VERIFIED`, never an invented result. Record conflicts and ask for an owner semantic decision only when the explicit recovery direction and existing contracts cannot resolve them.

## Inspected baseline and issue register seed

Planning inspection: 2026-09-08, `/root/signalix`, branch `release/signalix-mvp-stable`, HEAD `2192edb405e639d7d29fd6ae287c93ade3ce813b`. Status names the local origin tracking branch; no fetch or remote freshness claim. Eight tracked dirty files total **565 insertions / 92 deletions**; `git diff --check` passed before plan creation. Tests and live probes were not executed in planning. Test source and production callers were inspected. The supplied live observation (`daily_status=fresh`, `intraday_status=fresh`, root `status=unknown`) is a reported symptom, not a newly verified response.

| ID | Evidence / issue | Required disposition |
|---|---|---|
| B1 | Eight dirty tracked files and nine worktrees; synchronized HEAD is not a captured release | Wave 1 inventory, Wave 2 durable baseline, Wave 7 retention proof |
| R1 | `playbook_52w.py:_generate_52w_events_validated()` sets C3 entry index to signal index + 1; `_regime_contexts()` maps entry date; `_market_regime_at_entry_validated()` includes SET Close through that index | Wave 3 explicit decision-information cutoff and entry-day mutation regression |
| R2 | Current-237 `52W-C3_SUPPORTIVE_ONLY` reports `+0.2209R`; interrupted fold edits are present, not independently verified | Quarantine conclusion now; Wave 4 corrected evidence; never tune to recover the number |
| T1 | `backend/team_facts_api.py:build_history_response()` replaces missing 60m `run.as_of` with Daily date while declaring `intraday_price_data` | Wave 5 requested-source null timestamp regression/fix |
| D1 | Pipeline says Team Facts runtime not deployed; Deployment records public no-auth promotion on September 3 at 20:43 ICT and freshness re-gate at 21:02 ICT | Preserve dated evidence, remove conflicting current claim; current runtime awaits Lite |
| F1 | Reported canonical components fresh/root unknown; projection preserves supplied root metadata, sidecar updates fetch lineage only | Wave 6 trace contract first; do not infer aggregate health from two fields |
| F2 | Team Facts `_item_freshness()` uses seven-day Daily/three-day intraday elapsed-age thresholds; canonical freshness has session-aware behavior | Document separate meanings; no silent calendar/threshold substitution |
| D2 | Pipeline retains Bee final-gate and VCP-first active probes; Architecture/Deployment mix old DB-built and current published-model descriptions | Reconcile current routing/acceptance, retain historical milestones |
| S1 | Chart route remains in legacy dispatcher; pagination generation mixing and old cache removal are advisory follow-ups | Record deferred, no implementation in recovery |

### Owner-owned tracked changes

| Path | Provisional classification |
|---|---|
| `backend/mvp_routes.py` | Retain/review public Team Facts no-auth route removal; no policy change |
| `backend/team_facts_api.py` | Retain/rebaseline facts thresholds, per-symbol freshness and producer audit isolation; known history defect |
| `backend/test_team_scan_api.py` | Retain/rebaseline matching Team Facts regression coverage |
| `docs/START-HERE.md` | Retain navigation/document-skill boundary addition |
| `vault/Architecture.md` | Retain Team Facts contract; reconcile requested-source and current serving wording |
| `vault/Deployment.md` | Retain dated Team Facts promotion evidence; clarify current versus historical claims |
| `vault/Execution-Pipeline.md` | Retain facts contract additions; reconcile deployment and final-gate contradictions |
| `vault/INDEX.md` | Retain research/skill-boundary navigation; reconcile baseline routing |

### Nine registered worktrees

Paths below are relative to `/root/signalix` except the canonical root. `git cherry release/signalix-mvp-stable HEAD` reports `-` for the listed divergent commits; patch equivalence alone is insufficient for retirement.

| Worktree | HEAD | Inspected state and initial disposition |
|---|---|---|
| `/root/signalix` | `2192edb` | Canonical bind mount; eight dirty paths plus artifacts; retain |
| `.scratch/codex-intraday-chart` | `b822dda` detached | Nine dirty source/test/unit/docs paths; untracked `IMPLEMENTATION_BRIEF.md`, `backend/chart_rows.py`; preserve |
| `.worktrees/daily-structure-main-ui` | `fd716ea` detached | Tracked-clean; one release-absent patch-equivalent commit; parity-review/archive candidate |
| `.worktrees/kanban-20260901-tickets` | `2535dac` detached | Dirty `backend/mvp_api.py`, `backend/test_setup_candidates_api.py`; review scratch; preserve |
| `.worktrees/kanban-20260903-ploy-setup-review` | `d414e7c` feature/setup-candidate-review-fixes | Eleven dirty engine/contract/UI/tests; two untracked probes; preserve, policy work frozen |
| `.worktrees/research-runner` | `2192edb` detached | Entire `prototypes/research_to_promotion/` untracked, including interrupted fold work; retain active research boundary |
| `.worktrees/signalix-20260901-codex-flow` | `6529b02` detached | Dirty Architecture/Deployment and many scratch handoffs; nine patch-equivalent divergent commits; preserve artifacts |
| `.worktrees/team-facts-api-fix` | `a59f4f0` detached | Dirty route, untracked Team Facts module/tests; compare against release dirty implementation, preserve |
| `.worktrees/w3-publication-gate` | `717b7c7` detached | Tracked-clean; one patch-equivalent divergent commit; parity-review/archive candidate |

Top-level untracked groups requiring individual manifest entries: `.scratch/2026-09-02-2205-signalix-refactor-deploy-close-handoff.md`, `.scratch/2026-09-03-1005-ui-quote-completeness-handoff.md`, `.scratch/2026-09-03-1130-worktree-cleanup-handoff.md`, `.scratch/2026-09-03-1505-wave24-lite-fixes-handoff.md`, `.scratch/codex-intraday-chart/`, `.scratch/review-cockpit-mockup.html`, `.scratch/worktree-cleanup-2026-09-03/`, `docs/current/2026-09-03-signalix-skill-document-boundary.md`, `docs/current/2026-09-05-wave-playbooks-research-findings-handoff.md`, `docs/current/2026-09-05-wave-playbooks-research-project.md`, `docs/current/2026-09-08-0915-52w-research-session-closeout.md`, `docs/wayfinder/research-to-promotion/`, `review_2026_09_03.json`, `review_2026_09_03.py`, `vault/Research-Index.md`, `vault/Research-Playbook-Bible.md`. Nested and ignored contents remain unassessed until manifest enumeration; do not assume directories contain disposable output.

## Wave 1 — Freeze and inventory without mutation

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** none. **Files:** read-only Git/source metadata, then input to Wave 2; no commit.

- [ ] Capture `git status --short --branch`, `git rev-parse HEAD`, `git rev-list --left-right --count HEAD...origin/release/signalix-mvp-stable`, `git worktree list --porcelain`, `git diff --stat`, `git diff --name-status`, `git diff --check`.
- [ ] For each of the nine literal paths above, run `git status --short --untracked-files=all` from that worktree directory, and enumerate ignored filenames there using `git ls-files --others --ignored --exclude-standard`. These are metadata operations; do not open excluded/secret payloads. Record symlinks and nested repositories without following them into sensitive locations.
- [ ] Inspect allowlisted source/docs/test diffs per worktree. For every enumerated path, assign retain-active-source, retain-evidence, interrupted-unverified, patch-parity-review, or restricted-uninspected-preserve. No artifact remains silently disposable. Include ownership, relative path, HEAD, tracked state, source provenance and reason.
- [ ] Record `git log --oneline HEAD --not release/signalix-mvp-stable` and `git cherry release/signalix-mvp-stable HEAD` in each worktree; inspect changed-file sets for matching patches. Preserve detached commit reachability before any later retirement.
- [ ] Recheck status. Acceptance: all eight tracked paths, all nine worktrees and all enumerated untracked/ignored entries have a classification; no file content or registration changed. Lite reviews the inventory before adopting any owner dirty patch.

## Wave 2 — Durable documentation baseline

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** Wave 1. Perform documentation edits in the isolated recovery candidate; reuse that candidate for Wave 5 after the docs gate. **Create only during implementation:** `docs/current/2026-09-08-signalix-current-baseline.md`. **Modify:** `docs/START-HERE.md`, `vault/INDEX.md`, `vault/Execution-Pipeline.md`, `vault/Architecture.md`, `vault/Deployment.md`. No source, research payload or worktree moves.

- [ ] Use the new baseline record for the complete artifact manifest and durable issue matrix: ID, severity, evidence, source/runtime distinction, exact affected paths/symbols, owner, wave, acceptance condition, disposition and remaining blocker. Include all Wave 1 entries, Git hashes and restricted-uninspected preservation decisions. This is a control record, not a competing product spec.
- [ ] Documentation RED: run `rg -n 'runtime not deployed|Bee|live DB-built|live DB builder|api/vcp-finder' docs/START-HERE.md vault/Execution-Pipeline.md vault/Architecture.md vault/Deployment.md`. Expected finding: contradictory active Team Facts deployment statement and stale current-gate/serving wording. This is a document audit, not a fabricated failing unit test.
- [ ] Establish one current baseline section that points to the control record. Keep September 1–3 PASS claims explicitly dated and historical; state today's runtime/API/UI as `NOT VERIFIED`. Describe published-model request serving accurately. Preserve the no-auth owner decision and historical authenticated milestones. Use Lite/current setup API for active acceptance; retain VCP probes only as compatibility/history.
- [ ] Record R1/R2 as blocking research inference and freeze new tuning. Link the research authorities without importing research policy into product authority. Record root-unknown freshness as unresolved, not automatically wrong.
- [ ] GREEN: repeat the targeted scan and manually verify every remaining match is intentionally historical/audit. Check every new relative Markdown link resolves. Inspect `git diff --` for the six allowlisted files and run `git diff --check`; compare complete changed-file inventory with Wave 1 so owner additions are not dropped.
- [ ] Lite reviews the exact staged docs hunks and existing dirty-doc adoption. Local commit point: `docs: establish Signalix recovery baseline`. Stage only the six named files/hunks; exclude research authorities until their own docs wave. No runtime readiness assertion or deployment follows this commit.

## Wave 3 — Research cutoff repair with TDD

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** Waves 1–2. **Workdir:** `/root/signalix/.worktrees/research-runner`. **Source/test allowlist:** `prototypes/research_to_promotion/playbook_52w.py`, `prototypes/research_to_promotion/run_52w_deep_research.py`, `prototypes/research_to_promotion/test_playbook_52w.py`.

- [ ] First rebaseline the untracked research package. Review all ten Python source/test files listed by `rg --files prototypes/research_to_promotion -g '*.py'`; distinguish interrupted `symbol_fold()`, `_fold_readiness()` and readiness tests from accepted timing code. Run existing research tests before edits with `PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider prototypes/research_to_promotion -q`. Record pre-existing failures separately. If cleanly separable, leave incomplete folds untouched; otherwise Lite narrows a prerequisite repair before timing work. Never label partial fold work accepted merely because it exists.
- [ ] Lite approves a source-only preservation manifest for the untracked package before staging. Capture reviewed pre-fix source/tests in a distinct research-baseline local commit, `chore(research): capture reviewed recovery baseline`, only after tests/review. Retain incomplete source in place or a separately labeled archive; do not accidentally package it as accepted. Include required imports/support modules in the manifest rather than committing three files with missing dependencies.
- [ ] Add `test_next_open_regime_ignores_entry_day_set_close` to `test_playbook_52w.py`: keep stock events fixed, make prior SET close supportive and entry-date close defensive, then reverse only the entry-date close. Assert C3 supportive admission, context and cutoff remain identical. Exercise `_regime_contexts()` and `summarize_policy()`, not just the low-level index helper.
- [ ] RED: `PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider prototypes/research_to_promotion/test_playbook_52w.py -k next_open_regime_ignores_entry_day_set_close -q`. Expected failure: entry-day close changes selected regime/admitted event set. A missing import or empty event fixture is not valid RED evidence.
- [ ] Add tests named `test_regime_cutoff_separates_open_and_close_on_same_date`, `test_regime_cutoff_missing_session_is_not_verified`, and `test_unfiltered_event_identity_survives_regime_cutoff`. Cover future-row mutation, weekend/holiday gap, missing prior SET session, insufficient history, missing next open and mixed entry semantics sharing an entry date. Assert no shared cache entry across differing cutoffs. Unfiltered event IDs, signal/entry indices, prices, stops and evaluator outcomes must remain equal.
- [ ] Smallest implementation: introduce an explicit event decision cutoff in `_generate_52w_events_validated()` and consume it in `_regime_contexts()`. For next-day open, use the completed **signal-session SET close**, with exact signal-date matching; do not decrement a stock index into SET or invent a calendar-day predecessor. Require that the selected date is strictly before entry date. Missing exact cutoff context yields `NOT_VERIFIED`, not a stale nearest-row fallback. Existing same-day-close/retest-close policies retain entry-date close evaluation as their declared close-simulation convention; label that convention and do not claim guaranteed executable closing fills.
- [ ] Keep `_market_regime_at_entry_validated()` as arithmetic through the selected information index; clarify its docstring. Cache contexts by cutoff date and entry semantics. Expose cutoff date/boundary and selected SET `as_of`; change misleading `SET_exact_entry_date`/manifest mapping metadata to the explicit cutoff contract, version/hash the semantic change. Preserve evaluator axes, regime thresholds, event identity inputs and holdout selection rules.
- [ ] GREEN: run the complete `test_playbook_52w.py`, then the whole `prototypes/research_to_promotion` suite with the same pytest flags. Run `python prototypes/research_to_promotion/run_52w_deep_research.py --synthetic-smoke --output /tmp/signalix-baseline-recovery-52w-synthetic.json`; this synthetic output must not replace prior manifests. Inspect full source diff and `git diff --check`.
- [ ] Lite independently reproduces RED/GREEN and checks exact three-file repair scope relative to the reviewed research baseline. Local fix commit: `fix(research): bound SET regime evidence to entry information cutoff`. No production cherry-pick/deploy.

## Wave 4 — Corrected research evidence, then research documentation

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** Wave 3 accepted. **Research execution files:** no source changes. **New durable docs:** `docs/current/2026-09-08-52w-cutoff-replay-evidence.md`. **Docs sync:** `vault/Research-Index.md`, `vault/Research-Playbook-Bible.md`, `docs/current/2026-09-08-0915-52w-research-session-closeout.md`. Separate from research code commits.

- [ ] Lite reviews `/tmp/signalix_four_playbook_baseline.py` as loader source before using `main()` outside synthetic mode; it is a dynamic dependency, not a result payload. Record loader checksum, exact read-only input location/permissions, frozen symbol-set hash and data-window hash in the evidence packet. If safe frozen inputs cannot be established within permitted scope, mark real replay `NOT VERIFIED` and do not invoke the loader. Do not query production DB to fill the gap.
- [ ] Archive approved old result manifests/dashboard opaquely with checksums under `docs/archive/research/2026-09-08-52w-pre-cutoff/` only after Lite scopes those exact artifacts and validates they contain no restricted data. Never overwrite `/tmp/signalix_52w_cycle3_current237.json`. Metadata-only retention is acceptable when payload inspection is prohibited; current retention defaults to leaving originals intact.
- [ ] Freeze the same current-237 retrospective universe, as-of `2026-09-04`, train/validation/locked-holdout splits, all existing comparators, 0/25/50 bps and 2.5/3/3.5/4/5R axes before replay. Preserve blocked `3BBIF`, `COM7`, `PR9` or explicitly report input drift. Freeze a reviewed symbols-only input at `/tmp/signalix-baseline-recovery-current237-symbols.json`.
- [ ] Real replay command, only after the loader/input gate: `python prototypes/research_to_promotion/run_52w_deep_research.py --symbols-json /tmp/signalix-baseline-recovery-current237-symbols.json --output /tmp/signalix-baseline-recovery-52w-corrected.json`. Record exact interpreter, source commit, loader/input/policy/manifest hashes, dates, exit status and resource use. If the loader cannot reproduce frozen inputs, stop comparison rather than silently accepting current data.
- [ ] Independently compare generated versus admitted events, unfiltered identity parity and cutoff compliance. Report train, validation and locked holdout separately, regime counts, sample sizes, undecided statuses, concentration and cost axes. Existing fold output is only evidence if its interrupted implementation was independently accepted; otherwise explicitly keep fold readiness unverified. Never select policies or months using corrected holdout.
- [ ] Evidence RED/GREEN: old `+0.2209R` fails the entry-information validity gate regardless of its numeric value; corrected evidence passes only when every next-open context predates entry and independent recomputation agrees. A lower/negative result is a valid research outcome. Do not tune to restore `+0.2209R`.
- [ ] Update the three research authorities and new evidence note: preserve old cycles as superseded timing evidence, reconcile early C2 versus later C3 narratives, replace the next sequence with cutoff-first recovery results, retain `RESEARCH_ONLY`, `SHADOW_READY=NOT VERIFIED` until all separate gates pass, and production HOLD. If replay blocked, say so with exact missing input rather than repeating leader claims as current.
- [ ] Run `rg -n '0\.2209|exact.entry.date|potential.*leader|52W-C2|SHADOW_READY' vault/Research-Index.md vault/Research-Playbook-Bible.md docs/current/2026-09-08-0915-52w-research-session-closeout.md docs/current/2026-09-08-52w-cutoff-replay-evidence.md`; review every surviving historical claim and link. Run `git diff --check` and exact allowlist review. Local docs commit after Lite review: `docs(research): record cutoff-corrected evidence and limits`. Approved archives, if created, get a separate artifact-retention commit; never bundle raw outputs into the source fix.

## Wave 5 — Team Facts requested-source timestamp repair

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** baseline accepted; research repair/replay attempted and recorded first. **Candidate source/test files:** `backend/team_facts_api.py`, `backend/test_team_scan_api.py`. Existing `backend/mvp_routes.py` no-auth diff is adopted separately, unchanged, after baseline review.

- [ ] Reuse the isolated recovery candidate rooted at `2192edb` from Wave 2 and apply only Lite-reviewed Team Facts dirty source/test/no-auth patches. Record original and adopted patch hashes. Run from its `backend/`: `PYTHONDONTWRITEBYTECODE=1 /root/.venv_img/bin/python -m pytest -p no:cacheprovider test_team_scan_api.py`. Review/commit this owner-authorized baseline separately as `chore: capture reviewed public Team Facts baseline`; no unrelated dirty docs staged.
- [ ] Add parameterized `test_history_missing_60m_keeps_requested_source_as_of_null`: Daily present with no intraday rows, or only unfinished rows after a fixed UTC boundary. Assert `run.as_of is None`, `history.source == 'intraday_price_data'`, `timeframe == '60m'`, empty `completed_60m`, explicit unavailable freshness, and separately retained Daily baseline provenance. Add a route-level fixture using existing `Handler`/monkeypatch seams to prevent DB access.
- [ ] RED: `PYTHONDONTWRITEBYTECODE=1 /root/.venv_img/bin/python -m pytest -p no:cacheprovider test_team_scan_api.py -k history_missing_60m_keeps_requested_source_as_of_null`. Expected failure is Daily date versus null; no real SQL or clock dependency.
- [ ] Smallest implementation in `build_history_response()`: branch on requested timeframe. 60m uses `_latest_completed()` only; 1D uses latest sorted Daily date or null. Remove only the cross-source fallback. Keep list fallback, API keys/versions, public/no-auth behavior, thresholds and facts calculations unchanged.
- [ ] GREEN: run `test_team_scan_api.py`, then `test_team_scan_api.py test_setup_candidates_api.py test_legacy_routes.py test_read_model_publisher.py test_freshness_assessment.py test_source_freshness.py`. Cover valid 60m, missing Daily/valid 60m, both missing, out-of-order rows, invalid limits/timeframes, source metadata isolation and facts-only recursive filtering.
- [ ] Inspect exact two-file fix diff; `git diff --check`. Lite reviews before local commit `fix: preserve requested-source Team Facts history timestamp`. Sync the clarified null behavior into `vault/Architecture.md` and `vault/Execution-Pipeline.md` in a separate docs-only commit, `docs: clarify missing Team Facts history timestamps`, before closing this wave. Do not claim deployment.

## Wave 6 — Freshness contract reconciliation without policy tuning

**Worker:** Codex Astra; **gate:** Lite. **Dependencies:** Wave 5. **Read:** `backend/team_facts_api.py` (`_item_freshness`, `_classify_freshness`, `_freshness`, `build_response`, `build_history_response`), `backend/freshness_assessment.py:assess_projection_freshness`, `backend/canonical_freshness_lineage.py:overlay_latest_intraday_metadata`, `backend/canonical_setup_projection.py:project_setup_candidates_response`, `backend/mvp_routes.py:_handle_canonical_routes`, `backend/mvp_api.py`, `backend/frontend/canonical-client.js`, `backend/frontend/app.js`. **Write:** baseline record, Architecture/Pipeline docs and characterization tests `backend/test_team_scan_api.py`, `backend/test_setup_candidates_api.py`, `backend/test_freshness_assessment.py`; production implementation is conditional on the contract gate below.

- [ ] Trace the reported canonical `fresh/fresh/unknown` envelope from published metadata through overlay/projection into consumers. Record root status meaning, assessed scope, timestamps, missing/unknown evidence, immutable model generation and which fields consumers use. Use a synthetic fixture with that exact combination; do not edit published artifacts or assume two fresh components imply globally fresh data.
- [ ] Record a truth table for Team Facts list versus one-symbol history: fully fresh, mixed stale/missing, all unusable, unknown time, empty scope. Confirm producer metadata is only `source_metadata.reported`. Explain elapsed-age seven/three-day thresholds and hourly completion boundary separately from canonical session-aware freshness; preserve them. Record `daily[-2]` quote comparison as a deferred semantic question, not part of the timestamp repair.
- [ ] Contract gate: Lite determines whether root unknown is a valid distinct uncertainty or a violated documented aggregate contract. If authorities conflict, record the affected symbols/files and obtain Arm's decision before production edits. Do not manufacture a failing assertion that the root must be fresh. If uncertainty is valid, documentation plus passing characterization completes this wave.
- [ ] If a concrete aggregation defect is established, Lite scopes a follow-on source slice within `backend/freshness_assessment.py`, `backend/canonical_setup_projection.py`, or `backend/canonical_freshness_lineage.py`, selecting only the proven owner. Add `test_canonical_freshness_preserves_documented_aggregate_contract` to `backend/test_setup_candidates_api.py` using the agreed truth table. RED command from `backend/`: `/root/.venv_img/bin/python -m pytest test_setup_candidates_api.py -k canonical_freshness_preserves_documented_aggregate_contract`; expected failure must name the agreed semantic mismatch. Fix only that ownership seam; never blanket-copy Team Facts aggregate rules into canonical status.
- [ ] GREEN/characterization: run `test_team_scan_api.py test_setup_candidates_api.py test_freshness_assessment.py test_source_freshness.py test_read_model_publisher.py`; verify immutable generation/Daily provenance and per-symbol lanes do not change due to a fetch-only sidecar. Run `git diff --check`, inspect complete diff and exact changed-file scope. If source repair is required, commit separately as `fix: reconcile documented canonical freshness aggregation` only after Lite review; tests-only characterization is `test: characterize Signalix freshness boundaries`.
- [ ] Docs-only commit after Lite review: `docs: reconcile canonical and Team Facts freshness semantics`, limited to the baseline record, `vault/Architecture.md`, `vault/Execution-Pipeline.md`. Record any unresolved semantic question as `NOT VERIFIED` and block dependent acceptance; do not expand this plan into threshold tuning.

## Wave 7 — Retain/archive after parity and reachability checks

**Worker:** Codex Astra for archive/control docs; **operator/gate:** Lite. **Dependencies:** preceding wave dispositions recorded. **Files:** `docs/current/2026-09-08-signalix-current-baseline.md`; approved non-secret archives under `docs/archive/recovery/2026-09-08/`. No product source edits.

- [ ] Re-enumerate all worktrees, status and artifact metadata, including any recovery candidate created during implementation. Compare against Wave 1; new paths get explicit classification. Check `df -h /root/signalix /tmp` before copying. No whole-tree tar or unrestricted patch archive that could include secrets/results.
- [ ] For each retirement candidate, verify patch parity (`git cherry`) and semantic file/test parity with accepted release/candidate, not just ancestry. Retain dirty experimental policies unchanged. Inspect approved source diffs from Team Facts and chart worktrees without promoting unrelated refactors.
- [ ] Preserve each detached HEAD with a named `archive/2026-09-08/` ref under explicit local archive scope; verify reachability with `git cat-file -e` and `git show-ref`. Store approved source patches, filenames, checksums and dispositions in the archive manifest; verify copies byte-for-byte. Do not read/copy restricted entries; retain their original worktree in place.
- [ ] Cleanup RED: a missing archive/checksum, unique unreachable commit, unclassified ignored path or unexplained semantic difference rejects retirement. GREEN: every artifact is retained in place or has verified approved archive location, every commit remains reachable, all manifest links resolve. Directory absence alone is never evidence of success.
- [ ] Default final disposition is retain/archive, with no worktree removal necessary. A normal `git worktree remove` may be separately scoped by Lite only for a tracked-clean, untracked/ignored-empty, parity-verified worktree after durable reachability checks. Any refusal means preserve; never retry with force, prune evidence or remove branches reflexively.
- [ ] Re-run worktree list, statuses, `git diff --check`; verify canonical mount path survives unchanged. Local archive/docs commit after Lite review: `chore: preserve and classify recovery artifacts`. Record exact retained/archived registrations and intentionally untouched evidence. No source/research/runtime change in this commit.

## Wave 8 — Lite independent full acceptance and bounded runtime gate

**Implementation worker:** Codex Astra supplies candidate and evidence; **independent operator/final gate:** Lite. **Dependencies:** all source/contract gates accepted; research may have a documented blocked real replay, but cannot be represented as validated or promoted. **Code edits:** none. **Evidence/docs:** `docs/current/2026-09-08-signalix-baseline-recovery-acceptance.md`, baseline record, `vault/Deployment.md`, `vault/Execution-Pipeline.md`.

- [ ] Lite checks the exact candidate commits and `git diff --name-status` against every wave allowlist, including adopted owner dirty changes. Independently run focused tests and the full relevant backend non-integration suite from candidate `backend/`: `PYTHONDONTWRITEBYTECODE=1 /root/.venv_img/bin/python -m pytest -p no:cacheprovider`. Run the complete research suite separately. Inspect integration fixtures before any explicit invocation; production DB tests are forbidden and disposable DB setup/writes need separate scope. Report skipped integration coverage honestly.
- [ ] Run `node --check backend/frontend/app.js`, `node --check backend/frontend/canonical-client.js` from candidate root; parse changed Python files with `ast.parse` without generating bytecode; run `git diff --check`. Record commands, exit codes, counts, warnings, pre-existing failures and exact source identities. Green source tests are not deployment acceptance.
- [ ] Read-only host/container preflight: `docker ps --format '{{.Names}} {{.Status}}'`; inspect only mount source/destination using a formatted `docker inspect` (never unrestricted environment/config output). Compare SHA-256 of allowlisted Python files and frontend assets on the canonical host with `/app/` files in `signalix_dashboard` and relevant backend files in `signalix_backend`; confirm actual mount destinations first. Compare the accepted candidate to host as a separate step. No secrets are hashed or printed. Record process start time/source mismatch; bind-mount hash equality alone does not prove loaded Python identity.
- [ ] Probe `curl -fsS --max-time 15 http://127.0.0.1:8000/health/readiness` and loopback/public `http://91.98.72.120:3001/mvp`, `/api/setup-candidates?limit=500`, `/api/team/setup-candidates`, `/api/team/setup-candidates/IRPC/history?timeframe=1D&limit=5`, `/api/team/setup-candidates/IRPC/history?timeframe=60m&limit=5`. Use header-free GET requests only. Record HTTP status, source generation, as-of/coverage/freshness, requested-source timestamps and explicit unavailable states. No database dump or direct SQL inspection. HTTP 200 alone is insufficient.
- [ ] If source differs or a loaded process needs refresh, Lite scopes promotion of only reviewed candidate patches into the canonical bind mount without overwriting other owner changes. Runtime reload is a separate final-gate operation: `docker restart signalix_dashboard` for dashboard Python changes. Recreate only if configuration/dependency wiring requires it and Lite documents the reason and affected services; no environment changes are in this recovery scope. Do not follow older blanket recreate or live alert-test instructions. No ingestion, migration, DB write or implicit publisher run. If sandbox/authorization prevents the required action, report the limitation and keep runtime `NOT VERIFIED`.
- [ ] After any reload repeat source identity, readiness and API checks. Verify canonical model remains primary, six lanes/universe lineage are honest, Daily versus completed-60m timestamps remain distinct, root freshness matches the resolved contract, Team Facts facts-only/public/no-auth behavior and list/history freshness agree for the same symbol. Exercise invalid timeframe (400), unknown symbol (404) and a synthetic missing-60m history regression; do not fabricate a production empty case by deleting data.
- [ ] Browser acceptance starts on the public URL at desktop 1440px and mobile 390px: load overview, filter, select actual candidate, open/scroll drawer, inspect Daily evidence and 1D/60m chart, change timeframe quickly, verify timestamps/candles/markers, readable text, controls, no horizontal overflow (`scrollWidth <= innerWidth`), and explicit unavailable chart state where applicable. Capture screenshots and DOM/network evidence. Arm retains semantic chart/trade judgment.
- [ ] Exercise one bounded failure → Retry → recovery using browser-local interception of `/api/setup-candidates`: fail one response, assert visible error/Retry and no false empty-success state, remove interception, click Retry and verify real restored data/cards/drawer. No server outage, file removal, service stop or DB mutation to simulate failure. End with normal public GET and drawer/chart checks.
- [ ] Documentation-only final commit after Lite review: `docs: record independent recovery acceptance`. Include UTC/ICT timestamp, environment, source/host/container/served identity, commands/results, browser metrics, failure/retry proof, deployment action actually performed, retained Git changes and unresolved research/contract gates. Keep historical runtime records intact. Lite gives `PASS`, `FAIL`, `REVISE` or `NOT VERIFIED` per layer and final verdict to Arm; missing runtime, API, freshness or browser evidence prevents production PASS.

## Handoff and completion checklist

- [ ] Each wave report names scope, rationale, changes, exact verification commands/results, runtime/deployment state, branch/commit and remaining owner changes, authority documents read, required/completed documentation sync, and Lite verdict.
- [ ] Verify the complete diff and reference scan for old current claims, timing metadata and decision labels. List intentionally untouched research payloads, restricted artifacts, deferred chart/cache/pagination/quote work and any unresolved authority conflicts.
- [ ] Confirm no push, migration, production DB writes, alerts, evaluator auto-caller, broker execution or automatic trade capability occurred. Record restart/recreate only if actually performed within Wave 8.
- [ ] Recovery is complete only when the baseline and artifact dispositions are durable, timing and requested-source fixes have independent evidence, freshness semantics are resolved or explicitly block acceptance, and Lite's final layer-by-layer verdict is delivered. A corrected research result need not be profitable; it must be honest.
